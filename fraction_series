#include<bits/stdc++.h>
using namespace std;
int main()
{
    float n, sum = 0;
    cout<< "Enter the last even number : ";
    cin>> n;

    for(int i = 1.5; i<=n;i++)
    {
        sum = sum + i;
    }
    cout <<sum;

    return 0;
}